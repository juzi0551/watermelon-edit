# 角色关系图谱系统 统一重构方案 (v2.4)

> 合并自：① 角色关系图数据模型与萃取链路重构 v1.0；② 人物图谱前端交互与视觉优化 v1.0；③ 校对 prompt「群体展开」规则；④ AI-Reader-V2 深度借鉴项（关系合并锁定策略、类型归一化体系、泛称过滤、前端交互增强）。
>
> 定位：**校对是核心业务，图谱可视化与关系挖掘借鉴 AI-Reader-V2 已验证的成熟模式，不再从零发明。**
>
> 范围：仅以新项目为验收口径；旧项目为历史数据，不迁移。
>
> 变更记录：v2.1 质量护栏详细设计；v2.2 预扫描/证据/阶段链/阵营；v2.3 Stage 1 注入规范 + 词典职责边界；v2.4 待评审 8 项全部落定——锁定防弱降级/允许强反转、后端输出 category、evidence 分级必填、突变阈值硬编码等。

---

## 一、总览：四个"是什么"

| 层 | 决策 | 借鉴来源 |
|---|---|---|
| 存储 | 事件日志（append-only，唯一事实源） | Event Sourcing |
| 投影 | 图为事件日志的派生投影（按对去重 + 快照） | Projection / CQRS |
| 合并 | **血亲/亲密锁定 + 频率投票**（非纯最新胜出） | AI-Reader-V2 entity_aggregator |
| 萃取 | 状态声明 + 群体展开 + 类型归一化 + 泛称过滤 | AI-Reader-V2 relation_utils / FactValidator |
| 前端 | 焦点邻域 + 分类过滤 + 强度滑块 + 逐跳路径 | AI-Reader-V2 GraphPage |

---

## 二、投影层与合并策略（后端核心）

### 2.1 事件日志（不变）

`character_relationships` 维持追加写入，作为关系事实唯一来源。读取路径外零改动。

### 2.2 投影聚合（按对去重，保留 v1.0 成果）

对每对 `(u,v)`（min/max 归一）折叠事件得到：

| 字段 | 来源 |
|---|---|
| `strength` | 事件总数 |
| `established_at` | 最小 `paragraph_idx` |
| `last_seen` | 最大 `paragraph_idx` |
| `paragraph_idx / paragraph_uuid` | **最新事件**（跳转锚点） |
| `type` | 合并策略产物（见 2.3） |

快照过滤：端点已登场（`first_appear_idx <= t`）且 `established_at <= t`。

### 2.3 合并策略升级（🔴 最重要借鉴项）

**现状（v1.0）**：`type` = 最新事件类型 → 隐患：血缘确立后被一条 neutral 互动降级。

**升级为 AI-Reader-V2 的三条规则（v2.4 细化"锁定防弱降级、允许强反转"）**：

```
对每对事件按时间排序，逐条应用：
① 血亲锁定：任一事件归一化为 family（父子/兄弟/夫妻等）→ 该对 type 锁定为 family
② 亲密锁定：任一事件归一化为 lover（恋人/结拜）→ 锁定为 lover
③ 非锁定（friend/enemy/neutral）：频率投票，出现次数最多的类型胜出

锁定语义（v2.4 决策）：
- 锁定 = 防"弱信号降级"：后续 neutral/friend 等弱互动不得冲掉 family/lover
- 允许"强反转"：若出现明确对立信号（如 enemy 事件）且证据充分（≥3 次或≥3 段），
  允许反转类型，并写入 stages 记录反转点 —— 反映"反目成仇"的剧情真实性
```

效果：`铜锁↔铜铃` 一旦被声明 family，后续任何 neutral 互动都不会冲掉兄弟边；但若剧情明确"兄弟决裂"，可被 enemy 强证据反转。

> **⚠️ 实际算法修正（对照 AI-Reader-V2 源码，非绝对锁定）**
> `entity_aggregator.py:275-314` 的真实逻辑比上面更精细，开发时必须按此实现：
> - **血亲证据需 ≥3 章**才生效（`_MIN_BLOOD_EVIDENCE = 3`，line 299），不足则退回频率投票
> - **亲密关系可覆盖弱血亲**：恋人证据 ≥ 血亲证据 ×3 时，取亲密（宝玉↔黛玉：兄妹1 vs 恋人58，line 306-314）
> - 血亲类型本身也在族内**按频率选最佳**（非"首次出现即锁定"，line 301-304）
> - **参考实现**：`backend/src/services/entity_aggregator.py:275-314`

### 2.4 关系类型归一化（借鉴 relation_utils）

引入归一化表，把 LLM 可能输出的同义类型折叠到规范枚举（5 类），支持「精确 → 包含 → 原样」三级匹配：

```
family  ← 父子/父女/母子/兄弟/兄妹/姐妹/叔侄/祖孙/夫妻/表亲/堂亲/亲家…
lover   ← 恋人/情侣/爱人/倾慕/结拜兄弟/义结金兰…
enemy   ← 敌人/仇人/死敌/对手/宿敌/逼婚…
friend  ← 朋友/好友/挚友/盟友/同门/师兄弟/同学/搭档…
neutral ← 其余/中立/其他
```

语义注意（AI-Reader-V2 踩过的坑）：
- **结拜兄弟 → lover（亲密），不是 family**
- **师兄弟/同门 → friend（social），不是 family，更不是师徒**——师徒才是层级关系（也归 family 之外）
- prompt 中作为 negative rule 显式声明「师兄弟≠师徒」

> **参考实现**：`backend/src/services/relation_utils.py`
> - 归一化表 `_RELATION_TYPE_NORM`：line 9-56
> - `normalize_relation_type`（精确→包含→原样）：line 70-79
> - 分类表 `_RELATION_CATEGORY`：line 81-113
> - `classify_relation_category`（关键词兜底）：line 115-130

### 2.5 统计元数据输出（供前端图例/滑块）

投影结果附带：
```
max_strength / suggested_min_strength     # 强度滑块建议值
type_counts / category_counts             # 图例计数
node 附加 degree（关联边数）               # 节点大小/登场度辅助
```

**v2.4 决策**：前端六分类由**后端直接输出 `category` 字段**（归一化层顺带算好，如 `family/intimate/social/hostile/neutral/hierarchical`），前端不做推导逻辑，未来扩展类型前端零改动。

### 2.6 阶段链输出（v2.2 新增，借鉴 RelationStage）

- 投影边附带 `stages` 字段：该对关系按时间序的类型演变链：
  ```json
  "stages": [
    {"type": "friend",  "from_para": 25,  "to_para": 300},
    {"type": "enemy",   "from_para": 301, "to_para": 512},
    {"type": "family",  "from_para": 513, "to_para": null}
  ]
  ```
- 来源：原始事件日志按 `paragraph_idx` 排序折叠（连续同类型合并为一段），与合并策略共用同一份聚合。
- 用途：前端关系详情展示「关系演进史」；快照快进时边类型变化的可视化基础。
- **参考实现**：`entity_aggregator.py:345-360`（连续同类型 stage 合并）

### 2.7 阵营可读化（低优先级，可选）

- 当前 Louvain `community_id` 为无名数字；可选为其生成可读标签（如"智星小团体""刘家"）或允许用户在 UI 重命名。
- 依赖前端「阵营/社区」展示层，标注为 P1 可选，不影响核心链路。

---

## 三、萃取链路（prompt + 质量）

### 3.1 prompt 已有（v1.0/1.2 保留）
- `relationship_events` = 关系状态声明
- description/delta_summary 揭示持久关系必须同步声明边
- `character_id` 确定性匹配落库

### 3.2 群体展开（补充，修复缺边）
- 文本明确 N 人同属群体（"三人形影不离的好伙伴"/"三兄弟"/"一家人"/"同门"）→ 必须为群体内**每一对**声明边，生成 C(N,2) 条，不得只输出主角相关对
- JSON 示例加三人三角形（智星/铜锁/哈呼 三条 friend）

### 3.3 类型归一化 + 负例（承接 2.4）
- prompt 输出允许更丰富的中文类型，落库前经 2.4 归一化
- negative rules：「师兄弟≠师徒」「结拜≠血缘」「泛称（群X/众X/X等）不得当作角色」

### 3.4 质量护栏详细设计（v2.1 细化，借鉴 AI-Reader-V2）

> 统一落点：新建 `backend/app/core/quality_rules.py`，所有规则单一入口，proofer 落库前调用。**白名单优先于黑名单**：已登记为角色的名字一律豁免。

#### 3.4.1 泛称 / 称谓过滤（防假角色，借鉴 `is_blocked_name`）

新角色 `name` 命中任一规则 → 不建节点：

```
① 姓+数字排行称谓：len>=3 且 name[-1] ∈ "爷奶" 且 name[-2] ∈ "大二三四五六七八九"   # 王大爷/刘二哥
② 结构称谓：len>=4 且 endswith("奶奶"/"姑娘"/"丫头"/"夫人"/"娘子"/"媳妇儿")
③ "老"前缀泛称：startswith("老") 且 len>=3（排除已登记，如"老子"）
④ 指示代词：len∈[2,4] 且 name[0] ∈ "那这"                                        # 那人/这小子
⑤ 姓+氏：is_surname_plus_shi（len==2 且 name[1]=="氏"）                            # 李氏/王氏
⑥ 集合引用：endswith("等") 或 startswith("群"/"众")                                # 贾母等/群妖
⑦ 长描述性名：len >= 10（疑似幻觉拼接）
⑧ 纯称谓未登记："太太"/"老者"/"老太婆" 等，且不在白名单
```

> **参考实现**：
> - `backend/src/services/name_authority.py`：`CANONICAL_BLOCKLIST` line 177、`is_surname_plus_shi` line 235、`is_blocked_name` line 392-430（①②③④⑤⑧ 的精确条件）
> - `backend/src/services/visualization_service.py`：`_skip_person_name` line 138-166（集合引用/长名/泛称的运行时过滤）

#### 3.4.2 幻觉孤岛过滤（防预训练泄漏，借鉴 `hallucination_filter`）

- **语料**：当前项目文档全文（`paragraphs` 表已可查，按 `(project_id, 文档版本哈希)` 增量缓存）。
- **判定**：角色主名 + 所有别名均未在语料中出现（名字长度 >= 可验证阈值，如 2 字），且非白名单 → 标记 `ungrounded=True`。
- **处理**：不删数据，图中以灰色虚环标注并提示「原文未检索到该角色」，由用户决定是否保留——避免硬删误伤。
- **缓存**：与 graph 缓存同生命周期，随段落增删失效。

> **参考实现**：`backend/src/services/hallucination_filter.py`
> - `MIN_VERIFIABLE_LEN = 2`（line 29）：<2 字名字不可验证，不判幻觉
> - `find_ungrounded_names`（line 61-80）：主名+全部别名均未在语料出现 → 无依据
> - `get_ungrounded_persons`（line 81-120）：带缓存 + 按文档版本失效

#### 3.4.3 canonical 选名 + 冲突裁决（防分裂，借鉴 `pick_canonical`）

落库时同一别名指向多个主名（alias 冲突）：

```
① 词典/已登记主名唯一 → 直接用
② 多候选 → 干净名优先（非 3.4.1 称谓/绰号、len>=2），全名（>=3字）再优先
③ 冲突裁决：保留"出现频率高"的映射；被丢弃方降为别名
④ 短称呼（那小子/老者）不作为主名候选
```

> **参考实现**：
> - `backend/src/services/name_authority.py`：`pick_canonical` line 436-480（dict_primary → 干净名频率 → 3字全名优先 的四级优先级）
> - `backend/src/extraction/name_resolver.py`：`load_from_entity_dictionary`（line 42-88，alias→canonical 建图）、`accumulate_from_chapter`（line 90-111，跨章别名冲突时保留频率高者）

#### 3.4.4 关系突变检测（借鉴 ProfileQualityChecker）

投影层对每对关系按时间序扫描 type 变化：

```
短窗口（≤3 段，硬编码常量）内 type 来回翻转（friend→enemy→friend）→ 标记 suspicious=True
```

处理：图中该边加警示色/符号，hover 提示「关系类型在短窗口内反复变化，可能为萃取噪声」，供人工复核；不做自动纠正。

> **v2.4 决策**：阈值硬编码 `MUTATION_WINDOW = 3`（常量），不设设置项（内部质量规则，无题材维度，单一阈值足够）。

> **⚠️ 实现口径修正（对照源码）**：AI-Reader-V2 的 `profile_quality_checker.py` 是**自动修正**而非标记——
> `fix_relation_mutations`（line 60-108）：统计各类型占比，把 minority stage 的 type 直接改写为主导类型，且**按题材放宽**（修仙/武侠容忍度更高，line 118）。建议我们取**折中**：先做标记版（v2.1 原方案，安全），不做自动改写。

#### 3.4.5 确定性规则库（FactValidator 轻量版）

落库前强制检查（命中则纠正或丢弃）：

```
① 师兄弟/同门 → friend（不是 family、更不是师徒）
② 结拜兄弟 → lover（不是 family）
③ 自引用（from == to）→ 丢弃
④ 类型无法归一化且为空 → 丢弃该条事件（不降级为 neutral 掩盖问题）
⑤ 泛称命中 3.4.1 → 丢弃该条事件
```

> **参考实现**：`backend/src/extraction/fact_validator.py`
> - `_is_generic_person`（line 1043）：泛称/集合引用判定（genre 感知）
> - `validate`（line 1252）：整章 Fact 校验入口（自引用、泛称、空类型等规则集中于此）

#### 3.4.6 与随文架构的适配说明

| AI-Reader-V2 做法 | 我们适配 |
|---|---|
| 全书级批量语料 | 当前文档全文（增量缓存，随段落增删失效） |
| 章节区间过滤 | 段落 Slider 快照 |
| 一次性聚合后校验 | 落库前规则检查（3.4.1/3.4.5）+ 查询时投影（3.4.3/3.4.4） |
| 用户 overrides 层 | v2.1 不引入 UI；白名单豁免承担同等兜底作用（后续可按需加） |

### 3.5 预扫描实体词典（v2.2 新增，jieba）

> 目的：填补"先建档后提取"差距——让 Stage 0/Stage 1 在提取前就知道全书可能存在的角色名与别名变体，防漏检、防分裂。

- **触发**：文档导入时，`asyncio.to_thread` 后台跑一次（CPU-only，不阻塞导入）。
- **产出**：新表 `entity_dictionary`（`project_id, name, frequency, source`），来源三类：
  ```
  source = jieba | ngram | dialogue
  - jieba：词性标注词频，保留 2-8 字名词
  - ngram：2-4 字 n-gram 词频（补 jieba 漏掉的），min_freq=5
  - dialogue：对话引号内人名提取
  ```
- **用法**：
  1. **仅注入 Stage 0 识别表**：未登记但全书高频出现的候选名，帮助识别漏检角色
  2. canonical 候选来源：`pick_canonical` 的词典主名优先级（§3.4.3 ①）
  3. 频率供别名冲突裁决
- **⚠️ 职责边界**：实体词典**只进 Stage 0**（识别任务）。**不得进入 Stage 1 主校对上下文**——词典候选是"可能登场"而非"确认登场"，混入会污染画像合成与关系声明（详见 §3.7 注入边界）。
- **依赖**：`requirements.txt` 加 `jieba`。
- **参考实现**：`backend/src/extraction/entity_pre_scanner.py`（`_scan_word_freq` line 304、`_scan_ngrams` line 343、`_extract_dialogue_names` line 370）+ `backend/src/db/entity_dictionary_store.py`

### 3.7 Stage 1 注入规范（v2.3 新增，职责分层 + 注入边界）

> 原则：**Stage 1 只允许看到"本窗口确认登场的角色 + 它们彼此之间的状态"**。多了是噪音（污染合成），少了是信息不足（漏边漏事件）。

#### 职责分层（数据流向）

```
实体词典 ──→ Stage 0 识别（判断"谁登场了"）
Stage 0 确认登场 ──→ Stage 1 萃取（画像/关系/事件）
词典候选 / 未登场角色 ──→ 不进 Stage 1 ❌
```

#### Stage 1 注入清单（仅 4 块）

| 区块 | 内容 | 服务任务 |
|---|---|---|
| ① 登场角色画像 | `[character_id] 主名（别名）[role] 全局画像：description`（≤100字），仅登场角色 | description 合成（旧画像 + 新转折） |
| ② 登场角色近期履历 | 每登场角色最近 **1-2 条** delta_summary（段位倒序） | 模型感知"该角色最近发生了什么" |
| ③ 登场角色间现有关系 | 两端点均在登场集合内的边 `A —type→ B（确立于第 N 段）` | 关系状态增/改/确认 |
| ④ 疑新角色 | Stage 0 的 suspected_new（name + aliases） | 新角色建档 |

#### 明确不注入

- 未登场角色的任何信息（画像/关系/履历）
- 登场角色的**全量**履历（只取最近 1-2 条）
- 实体词典候选（只进 Stage 0）
- 与登场角色无关的剧情事件
- 登场角色与**未登场角色**之间的边（关系声明只涉及本窗口角色）

#### 兜底路径

- Stage 0 识别失败 / 返回空 → 回退 `nodes[:20]` 注入（保持现状兜底）
- 注入总量控制：画像 ≤ 登场角色数 × ~130 字 + 履历 ≤ 登场角色数 × 2 条 + 关系 ≤ 20 条，窗口内可控

### 3.6 关系证据 + 置信度（v2.2 新增，借鉴 evidence/confidence）

> 目的：每条关系带「原文依据 + 置信度」，提升图的可信度与可溯源性；前端点关系可看"为什么这么标"。

- **prompt**：`relationship_events` 每个条目新增：
  - `evidence`：原文 1-2 句摘录（≤50 字），作为关系依据
  - `confidence`：`high`（原文明确描述）/ `medium`（可推断）/ `low`（模糊暗示）
  - **v2.4 决策（分级必填）**：`confidence=high` 必填 evidence；`medium` 可选（有则填）；`low` 允许缺省。避免"不可省略"逼模型编造证据或膨胀输出。
- **落库**：`character_relationships` 表加列 `evidence TEXT`、`confidence TEXT DEFAULT 'medium'`。
- **投影输出**：边附带 `evidence`（最新事件）与 `confidence`；快照/合并策略不依赖置信度（仅展示）。
- **前端**：边 hover/详情展示"原文依据：…"，可跳转定位段。
- **参考实现**：`backend/src/models/chapter_fact.py:36-42`（RelationshipFact.evidence）+ 抽取 prompt line 40/145-148

---

## 四、前端交互与视觉（借鉴 GraphPage）

### 4.1 焦点角色交互（保留 v1.0 方案）
- 侧边栏角色 `Select showSearch` 切换焦点
- 焦点节点放大+金色描边；一跳邻居与直接边高亮；其余置灰
- 「关系清单」区块：焦点角色的直接边，可跳转段落/切焦点

### 4.2 节点与边可见性（保留）
- 节点 label 加衬底、字号 13
- 边去 `endArrow`、类型中文 label 常显、hover/click 详情

### 4.3 分类过滤与降噪滑块（新增，借鉴）
- **关系分类过滤**：图例处可开关六大类（family/lover/enemy/friend/neutral/…），后端 `category_counts` 驱动
- **强度滑块**：`min_strength` 过滤弱关系，默认用 `suggested_min_strength`
- 节点大小 = 中心度 + degree 辅助

### 4.4 路径逐跳说明（新增，借鉴）
- 最短路径高亮之外，底部/侧栏展示步骤面板：
  ```
  智星 —friend→ 铜锁 —friend→ 哈呼
  共 2 跳 · 总强度距离 0.5
  ```

> **参考实现**：`frontend/src/pages/GraphPage.tsx`
> - `bfsPath`（line 71-105）：BFS 最短路径（无权），配合 `pathSteps`（line 136）逐跳列出 `from→to→relation_type→category`
> - `findPath`（line 293-352）：高亮 + 步骤面板组装

### 4.5 hover 邻域高亮（新增，借鉴）
- hover 节点 → 轻量高亮其一跳邻居；点击才持久聚焦（与 4.1 互补）

> **参考实现**：`GraphPage.tsx:276-292`（`connectedNodes`：hover 节点 + 其一跳邻居集合）

### 4.6 布局自适应（新增，借鉴）
- d3-force 参数按节点数/度数动态缩放（charge/linkDistance）
- containment 力防节点飞出画布
- 图例（角色类型色 + 关系分类色 + 边宽=强度）

> **参考实现**：`GraphPage.tsx:370-413`
> - `d3Force("charge")` 按度数动态排斥（line 372-380）
> - `d3Force("link")` 距离按节点数缩放（line 382-390）
> - `containment` 约束力（line 392-407）、`zoomToFit`（line 413）
> - 分类过滤 `toggleCategory`（line 154）、强度滑块 `minEdgeWeight`（line 122/501-507）、`suggested_min_edge_weight` 后端建议值（line 211）、图例开关（line 475）

---

## 五、改动清单

| 文件 | 改动 |
|---|---|
| `backend/app/core/graph_engine.py` | 合并策略升级（锁定+投票）、类型归一化表、统计元数据输出、degree 计算、关系突变检测标记、**stages 阶段链输出** |
| `backend/app/core/entity_pre_scanner.py` | **（新增）**jieba 预扫描：实体词典（jieba/ngram/dialogue 三源） |
| `backend/app/core/quality_rules.py` | **（新增）**泛称过滤、幻觉孤岛、canonical 冲突裁决、确定性规则，单一入口 |
| `backend/app/core/proofer.py` | 落库前调用 quality_rules；**解析 evidence/confidence 落库** |
| `backend/app/core/database.py` | prompt：群体展开、负例、类型放宽、**evidence/confidence 字段**；`character_relationships` 加列；`entity_dictionary` 新表；settings 默认模板 |
| `backend/app/core/context.py` | **Stage 1 注入规范实现**：仅登场角色画像 + 近期履历(≤2条) + 登场角色间关系摘要 + 疑新角色；实体词典**不进此层**（§3.7） |
| `frontend/src/components/CharacterGraph.jsx` | 焦点交互、分类过滤、强度滑块、路径逐跳、hover 高亮、布局自适应、图例、ungrounded/suspicious 提示、**边详情显示原文证据与置信度、关系演进史（stages）** |
| `backend/requirements.txt` | 加 `jieba` |
| `backend/app/tests/*` | 锁定/投票、归一化、泛称过滤、幻觉孤岛、canonical 冲突、群体展开、**预扫描、evidence 落库、stages 折叠**、前端构建回归 |

## 六、验证计划

1. 后端：`pytest backend/app/tests/` 全过。
2. 前端：`npm --prefix frontend run build` 0 错误。
3. 数据场景验证：
   - family 确立后被 neutral 事件覆盖 → 仍显示 family（锁定生效）
   - 三人群体 → 3 条边（群体展开生效）
   - 师兄弟 → friend 而非 family（归一化生效）
   - "群学生/X等/李氏/王大爷" 泛称不产生节点（泛称过滤生效）
   - 角色主名+别名全文未出现 → 图节点灰色虚环 + 提示（幻觉孤岛生效）
   - 别名指向两个主名 → 频率高者胜出（canonical 冲突生效）
   - friend→enemy→friend 短窗口翻转 → 边警示标记（突变检测生效）
   - **导入后实体词典生成、Stage 0 命中未登记高频角色；词典不进入 Stage 1 上下文**（预扫描 + 注入边界生效）
   - **Stage 1 注入仅含登场角色**（画像/履历/关系/疑新），未登场角色零注入（注入边界生效）
   - **边带 evidence/confidence，前端可显示原文依据**（证据生效）
   - **边 stages 显示 friend→enemy→family 演进史**（阶段链生效）
4. 前端手动：焦点切换、分类过滤、强度滑块、路径逐跳、hover 高亮、边证据详情。

## 七、待评审问题 → 已决策记录（v2.4）

> 全部 8 项已落定，开发按此执行；如需变更走方案修订流程。

| # | 问题 | 决策 |
|---|---|---|
| 1 | 锁定 vs 频率投票边界 | **锁定防"弱信号降级"，允许"强反转"**：family/lover 不被 neutral/friend 弱互动冲掉；若出现明确对立信号（enemy）且证据充分（≥3 次或 ≥3 段），允许反转并写入 stages（§2.3 已细化） |
| 2 | 类型归一化表维护 | **不做 UI**：同义类型走"表 + contains 匹配"，内部维护 |
| 3 | 泛称过滤误伤 | **白名单优先于黑名单**：已登记角色豁免（§3.4.1 约束） |
| 4 | 前端六分类来源 | **后端直接输出 `category` 字段**，前端不做推导（§2.5 已补） |
| 5 | 幻觉孤岛删/留 | **保持"灰色虚环 + 提示、不硬删"**；一键剔除后置（不引入 overrides UI） |
| 6 | 突变检测阈值 | **硬编码 `MUTATION_WINDOW = 3` 常量**，不设设置项（§3.4.4 已补） |
| 7 | evidence 成本 | **分级必填**：high 必填、medium 可选、low 缺省（§3.6 已补） |
| 8 | 预扫描刷新策略 | **导入时跑一次**；校对中增删大量段落后，在"校对完成"时标记"词典可能过期"，提示用户可手动重扫，不做自动增量刷新 |
