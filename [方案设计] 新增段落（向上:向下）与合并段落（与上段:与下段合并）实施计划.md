# [方案设计] 新增段落（向上/向下）与合并段落（与上段/与下段合并）实施计划

本计划旨在为小说校对与编辑系统设计并实现 **新增段落**（向上插入/向下插入）与 **合并段落**（与上段合并/与下段合并）两大核心编辑功能。得益于此前完成的 `paragraph-uuid` 架构升级，插入与合并段落时，已有段落的 UUID 保持不变，错字标注、章节、角色演进图谱与剧情事件均能实现无缝重新绑定与平移。

---

## 用户审阅项 (User Review Required)

> [!IMPORTANT]
> **文本合并时的分隔符逻辑**：
> 默认两段文本合并时使用换行符 `\n` 进行连接。如果两段文本末尾/开头均无空白，亦提供去除多余换行直接拼接的兼容选项。

> [!IMPORTANT]
> **合并段落时的错字与章节处理**：
> - 被合并/删除的段落上的待处理错误 (`errors`) 将自动重定向转移到合并后的保留段落上（按新的字符偏移位置进行匹配，不满足的自动软标记作废 `is_obsolete = 1`）。
> - 若被合并的段落是某个章节的标题段落，合并后标题自动保留归属于新的合并段落。

---

## 拟议变更 (Proposed Changes)

### Backend Core (`backend/app/core/database.py`)

#### [MODIFY] [database.py](file:///Users/zhonglei/Desktop/Project/西瓜少年/backend/app/core/database.py)

1. **新增 `insert_paragraph_and_reorder(document_id, target_idx_or_uuid, position='below', text='')`**:
   - 解析目标段落的位置 `insert_idx`（`above` 插入在 `target_idx`；`below` 插入在 `target_idx + 1`）。
   - 为新段落使用 `generate_id()` 生成全新的 UUID。
   - 对 `idx >= insert_idx` 的原有段落进行倒序（`ORDER BY idx DESC`）平移，更新 `idx = idx + 1`，并维持各自的 `uuid` / `edit_note` 绝对不变。
   - 插入新段落到 `paragraphs` 表。
   - 自动平移关联表：
     - `errors`: `UPDATE errors SET paragraph_index = paragraph_index + 1 WHERE document_id = ? AND paragraph_index >= insert_idx` （已有错字按其 `paragraph_uuid` 严格绑定，不发生偏移错位）。
     - `chapters`: `title_paragraph_idx`, `start_idx`, `end_idx` `>= insert_idx` 的同步 `+1`。
     - `character_relationships` & `plot_events`: `paragraph_idx >= insert_idx` 的同步 `+1`。

2. **新增 `merge_paragraphs(document_id, target_idx_or_uuid, direction='below', separator='\n')`**:
   - 确定要合并的两段：保留段 `P_keep` 与将被移除段 `P_remove`。
     - `direction == 'below'`: `P_keep` 为当前段，`P_remove` 为当前段的下一段（`idx + 1`）。
     - `direction == 'above'`: `P_keep` 为当前段的上一段（`idx - 1`），`P_remove` 为当前段。
   - 将 `P_remove.text` 拼接至 `P_keep.text`，并合并两者的 `edit_note` 履历。
   - 将 `P_remove` 上关联的所有 `errors` 重定向到 `P_keep.uuid` 和 `P_keep.idx`。
   - 若 `P_remove` 为章节标题段落，重定向该章节的 `title_paragraph_uuid` 到 `P_keep.uuid`。
   - 将 `P_remove` 上关联的 `character_relationships` / `plot_events` 重定向给 `P_keep`。
   - 执行 `delete_paragraph_and_reorder(document_id, P_remove.idx)` 移除多余行并完成后续连续平移。

---

### Backend API (`backend/app/api/projects.py`)

#### [MODIFY] [projects.py](file:///Users/zhonglei/Desktop/Project/西瓜少年/backend/app/api/projects.py)

1. **新增 API 路由 `POST /projects/{project_id}/paragraphs/insert`**:
   - 接收 `target_idx` / `paragraph_uuid`、`position` (`above` | `below`) 和 optional `text`。
   - 校验项目锁状态与段落合法性，调用 `insert_paragraph_and_reorder`。

2. **新增 API 路由 `POST /projects/{project_id}/paragraphs/merge`**:
   - 接收 `target_idx` / `paragraph_uuid` 和 `direction` (`above` | `below`)。
   - 校验边界条件（如首段不可向上合并，尾段不可向下合并），调用 `merge_paragraphs`。

---

### Frontend Services & UI (`frontend/src/services/api.js` & `ReviewReader.jsx`)

#### [MODIFY] [api.js](file:///Users/zhonglei/Desktop/Project/西瓜少年/frontend/src/services/api.js)

- 增加 API 调用封装：
  - `insertParagraph(projectId, targetIdx, position, text, paragraphUuid)`
  - `mergeParagraphs(projectId, targetIdx, direction, paragraphUuid)`

#### [MODIFY] [ReviewReader.jsx](file:///Users/zhonglei/Desktop/Project/西瓜少年/frontend/src/components/ReviewReader.jsx)

1. **工具条动作菜单增强**：
   - 浮动工具条中增加 **「插入段落」** 下拉操作：
     - `向上插入新段落`
     - `向下插入新段落`
   - 增加 **「合并段落」** 下拉操作：
     - `与上一段合并` (当 `idx > 0`)
     - `与下一段合并` (当 `idx < max_idx`)
2. **段落间悬浮快速插入按钮 (Optional UX)**：
   - 当鼠标悬浮在段落间隙时，显示极简 `+ 插入段落` 微型按钮。
3. **完成响应与平滑滚动**：
   - 插入/合并成功后弹出提示，调用 `onReloadProject()`，并自动将视口聚焦回新增/合并后的目标段落。

---

## 验证计划 (Verification Plan)

### 自动化与单元测试 (Automated Tests)
写独立 Python 验证脚本：
1. 测试向上/向下插入新段落，验证原有段落的 UUID 保持不变、索引递增 1、错误/章节平移无错。
2. 测试与上段/与下段合并，验证文本拼接无误、旧错误自动重定向到保留段落 UUID。

### 手动与 UI 验证 (Manual Verification)
1. 在前端页面任意段落点击浮动工具条，执行「向上插入新段落」与「向下插入新段落」，确认界面实时渲染新段落且无报错。
2. 对相邻两段执行合并操作，确认文本正确连接、错字高亮与卡片定位正常。
